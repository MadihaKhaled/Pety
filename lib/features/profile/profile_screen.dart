
import 'package:flutter/material.dart';

class PetBotScreen extends StatelessWidget {
  const PetBotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Column(
      children: [
        Text('PetBot'),
      ],
    );
  }

}